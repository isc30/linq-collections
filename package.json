{
	"repository": {
		"type": "git",
		"url": "https://github.com/isc30/TsLinq"
	},
	"scripts": {
		"node-build": "tsc",
		"browser-build": "browserify test/UnitTests.ts -p [tsify] > bundle.js",
		"pretest": "npm run node-build",
		"test": "nyc mocha --slow 0",
        "report-coverage": "nyc report --reporter=text-lcov | coveralls"
	},
	"devDependencies": {
		"@types/node": "*",
		"@types/mocha": "*",
		"mocha": "*",
		"typescript": "*",
		"coveralls": "*",
		"nyc": "*",
		"browserify": "*",
		"tsify": "*"
	},
	"directories": {
		"test": "test"
	}
}
