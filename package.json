{
	"scripts": {
		"pretest": "tsc",
		"test": "nyc mocha",
		"prepare": "tsc",
		"posttest": "nyc report --reporter=json && codecov -f coverage/*.json"
	},
	"devDependencies": {
		"@types/node": "*",
		"@types/jasmine": "*",
		"mocha": "*",
		"typescript": "*",
		"codecov": "*",
		"nyc": "*"
	},
	"directories": {
		"test": "test"
	}
}